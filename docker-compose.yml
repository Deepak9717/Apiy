services:
  app:
    build: .
    command: npm run dev

    volumes:
      - .:/app:delegated
      - /app/node_modules

    ports:
      - "3000:3000"

    environment:
      WATCHPACK_POLLING: "true"
      CHOKIDAR_USEPOLLING: "true"